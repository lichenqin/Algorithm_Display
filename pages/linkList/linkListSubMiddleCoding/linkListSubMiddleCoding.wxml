<text decode="{{true}}" class= "linkListCoding-style">
  //头指针为空指针，要删除结点指针为ptr，遍历指针为sort\n
  sort=head;\n
  while(sort->next!=null) {\n
  &emsp;if(sort->next->data==wantedData) {\n
    &emsp;&emsp;break;
  &emsp;}\n
  &emsp;sort=sort->next;
  }\n
  ptr=sort->next;\n
  sort->next=sort->next->next;\n
  delete ptr;
</text>